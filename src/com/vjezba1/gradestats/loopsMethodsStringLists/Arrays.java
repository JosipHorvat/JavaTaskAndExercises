package com.vjezba1.gradestats.loopsMethodsStringLists;

public class Arrays {

    // SWAP PLACES OF 2 NUMBERS IN ARRAY
    /*
     Scanner reader = new Scanner(System.in);

        int[] numbers = new int[5];
        numbers[0] = 42;
        numbers[1] = 13;
        numbers[2] = 12;
        numbers[3] = 7;
        numbers[4] = 1;

        for (int number: numbers)
              {
                  System.out.println(number);
        }

        System.out.println("Which two numbers should we swap? ");
        int number1 = Integer.valueOf(reader.nextLine());
        int number2 =  Integer.valueOf(reader.nextLine());

        int helper = numbers[number1];
        numbers[number1] = numbers[number2];
        numbers[number2] = helper;

        for (int number: numbers)
        {
            System.out.println(number);
        }
     */

    // FIND NUMBER AT CERTAIN INDEX
    /*
     int index = 0;
        for (int number : numbers) {
            index++;
            if (number == number1){
                System.out.println(number +" is Number at index "+ (index -1));
            }
            }
     */

    //ARRAY AS PARAMETER OF METHOD
    /*
    public static void listElements(int[] integerArray) {
    System.out.println("the elements of the array are: ");
    int index = 0;
    while (index < integerArray.length) {
        int number = integerArray[index];
        System.out.print(number + ", ");
        index = index + 1;
    }

    System.out.println("");
}
     */

  //  Print in stars

}
